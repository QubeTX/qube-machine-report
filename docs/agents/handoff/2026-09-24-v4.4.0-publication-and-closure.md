# v4.4.0 publication and remaining closure

Recorded September 24, 2026 (America/Chicago); publication timestamp is
September 25 in UTC. This is the current continuation record. Earlier release
and physical-fleet ledgers remain historical evidence and are not superseded
where their hardware/UAC rows are still open.

## Published result

TR-300 **4.4.0** is published at immutable source/tag
`f49d50a256842ac6c677d1c743c553b3dd92717f`. Public GitHub release
[396235243](https://github.com/QubeTX/qube-machine-report/releases/tag/v4.4.0)
was published at `2026-09-25T02:41:06Z` with exactly 34 nonempty stable-name
assets. The exact unyanked crates.io package checksum is
`f9b47301f2e02db3b51221bd93af7530f7dfd770e20fb0d9dca1d9eb619d5402`.

Both `report` and `tr300` expose the full CLI on every supported distribution,
including maintenance actions. `report` forwards to the exact adjacent
canonical executable, preserving help/version output and exit status. Package
and ownership inventories include both commands; old single-binary ownership
never authorizes deleting an unrelated `report`. Windows updates account for
both loaded images; Apple archives and universal packages sign both commands.
Optional shell-profile auto-run no longer defines an alias.

Both Windows MSI editions embed the complete PolyForm Noncommercial 1.0.0
license, including Required Notice. Inno already uses `LICENSE`. macOS has a
component PKG and compatibility DMG without an EULA screen; no new license
acceptance flow was added.

## Exact release evidence

| Gate | Evidence |
| --- | --- |
| Implementation merge | PR #32, `64486fa46790e4a4ecaaca847bfcc626280b3746` |
| Publisher-policy correction | PR #33, final source `f49d50a256842ac6c677d1c743c553b3dd92717f` |
| Exact-main CI | `36084969867`, attempt 2, all 20 jobs passed |
| Trusted-OIDC crate publication | `36084969841`, attempt 2, exact unyanked bytes above |
| Apple identity preflight | `36082271439`, both architectures passed |
| Tagged Release | `36085847270`, all 13 jobs passed; private 24 assets |
| Windows assembly | `36086249950`, passed; private 30 assets |
| Private Windows validation | `36086484931`, attempt 2, all 16 jobs passed |
| Native Mac/final publication | `36086249996`, all 12 jobs passed; native Intel/ARM PKG lifecycles, DMG bridge, Linux smoke, sole 34-asset publication |
| Post-public Windows updater | `36087344916`, all nine jobs passed |

The initial untagged publisher `36082259983` rejected the newly paired binary
inventory before credential use/upload. No v4.4.0 package or tag existed at
that point. PR #33 repaired both the current fixed-target policy and historical
single-target adjudication, with executable fixtures against real manifests;
it did not widen credential scope or execute package code while credentialed.

The first final-main Windows PowerShell attempt hit an existing 10-second
timeout. Five local reproductions and the hosted retry passed without code
changes. The first private Windows validation attempt received a transient
empty JSON download; all 30 API asset records were read and verified before an
unchanged-source retry. These were bounded retries, not changes to immutable
release source or assets.

## Public acceptance already recorded

- `target/public-v4.4.0-audit.json`: all 34 GitHub asset SHA-256 digests/sizes,
  13 checksum sidecars, eight aggregate records, all six paired-command archive
  inventories, both compatibility alias pairs, exact source-rendered wrapper
  bindings, immutable tag/main identity, and stable/latest release identity.
  The byte-audit tool performs no installation or payload execution.
- `target/public-v4.4.0-msi-proof.json`: read-only database inspection of both
  downloaded MSI editions verifies exact complete license-control RTF and both
  command files. This is distinct from hosted installer lifecycle evidence.
- Downloaded Windows commands report `tr300 4.4.0`, expose identical full help,
  emit valid fast JSON, and render full ASCII reports. Public Windows workflow
  acceptance passed all nine jobs in `36087344916`.
- Local formatting, Clippy, all 234 tests, dependency audit, package/dry-run,
  workflow/ShellCheck, managed transaction, and extracted workflow fixtures
  passed. Independent runtime, migration, and package reviews resolved the
  confirmed ownership, rollback, parsed-inventory, and live-image defects.

Evidence files under `target/` are local ignored artifacts. The run IDs,
immutable source/checksum, and summaries in this handoff and `TESTING.md` are
the durable tracked record.

## Website scope

The related website is deployed as **1.21.8**, commit
`f137414f442e67bee5d3a79ad52e46bc84ab0e0f`, production run `6652671791`.
All four routes and MSI-before-EXE ordering for Global/Corporate native
choices passed actual Chrome verification. Versionless latest download links
remain. This deployment does not change the application release identity.

## Remaining work and task state

1. Post-public Windows `36087344916` passed all nine jobs. Windows updater
   and channel checks plus Linux/macOS smokes are accepted; `#r44` is complete.
   No release workflow acceptance remains pending.
2. Finish the protected follow-up documentation PR, its required checks, merge,
   and exact-main/idempotent-publisher verification. Keep `#q44` and milestone
   `#v44` open until those required checks are complete. Drafting these records
   is not evidence of PR completion.
3. `#v440` implementation and `#eula` published license verification are done.
   Preserve all immutable tags and existing asset bytes; this docs follow-up
   must not create another tag or republish v4.4.0.

Personal AMD64 Linux laptop, Raspberry Pi 4, and the Alienware's natural Global
MSI v4.1.3 → v4.2.2 UAC transition remain open under `#hw4`. Do not equate
hosted native runners, downloaded Windows smoke, or source/readback checks with
those physical/operator outcomes. Any findings from those separate tasks must
be handled through the normal reviewed fix-forward process.
