TT;DR: Ship a complete TR-300 v4.4 release in which `report` is a real PATH-level sibling command beside `tr300` across every supported distribution channel. The milestone remains open through public byte, lifecycle, and post-public verification.

## Why
The operator asked to release a new version that makes `report` available as a PATH-level command rather than only a shell-profile alias. The release must preserve TR-300's origin-aware update and uninstall safety across Cargo, managed shell/PowerShell, Windows native installers, and the signed universal macOS package.

## Scope
In scope: implementation, packaging, install/update/uninstall behavior, code signing/notarization coverage, protected PR/main/crates/tag/release workflows, exact public asset audit, and release-ledger closure. Existing `tr300` behavior, ownership markers, receipts, updater channel selection, stable asset names, and the 34-asset publication contract are preservation invariants.

Out of scope: adding a new persistent `tr300 config` command and independently open personal AMD64 Linux/Raspberry Pi hardware gates.

## Status
0/3 child tasks complete. Implementation is active on `codex/v4.4.0-eula`; release and public audit are dependency-gated.

## Completed

## Activity
- 2026-09-24 — user authorized a combined production minor release with full aliases on every supported system; report-only restriction superseded.
- 2026-08-25 23:01 — created from the operator's release order; split implementation, immutable release, and public audit into separate gates (agent: codex)
