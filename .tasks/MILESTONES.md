# Milestones

- [x] **TR-300 v4.1.3 origin-preserving fix-forward** - installer-first macOS, safe channel-preserving updates including Global Program Files handoff, and 30-asset release proof (completed 2026-07-18) #v41
- [x] **TR-300 v4.2 managed installation contract** - managed CLI defaults, authoritative fresh intent, direct universal PKG, legacy DMG bridge, and 34-asset hosted proof (completed 2026-07-18) #v42
- [ ] **TR-300 v4.3 battery, latency, thermals** - v4.3.7 is the current trusted-OIDC crate, while immutable v4.3.0 through v4.3.7 tags all failed to complete GitHub distribution: the first five stopped before draft creation on Windows cargo-dist hard-link handling, Bash-4-only Apple staging, the pinned cargo-dist two-LF raw checksum contract, its full POSIX tar-mode encoding, then jq boolean context; v4.3.5 created exact private draft `376242296` before its draft-blind by-tag lookup failed; v4.3.6 created exact private draft `376283574` before its managed-installer checksum lookup rejected default `sha256sum` field syntax; v4.3.7 created exact private draft `376309349`, then read-only Windows/macOS downstream tokens without push access could not see it and both deterministic source resolvers stopped before build/publication. The v4.3.8 fix-forward preserves Linux battery selection (`*_avg` plus signed current/power; historical Pi source unproven), deterministic fault-aware Linux CPU/GPU thermals including `soc_thermal`, Windows NVIDIA GPU-only thermals with CPU absent/null, and a measured 58.8% full-mode latency cut with no fast-mode gain claim. v4.3.8 review/CI/OIDC, the fresh tagged native chain, public-byte acceptance, deployment, and physical evidence remain independently open #v43
- [ ] **Personal hardware continuation** - real Windows, AMD64 Linux, and Raspberry Pi 4 verification #hw4
