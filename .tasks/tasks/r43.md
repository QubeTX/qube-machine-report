TT;DR: Security-harden, independently qualify, merge, publish, and deploy
TR-300 v4.3.0 from one accepted exact candidate.

## Why

The original v4.3.0 task combined PR repair with merge, crates publication,
tagging, and release qualification. The operator narrowed the current work on
2026-08-23 to repairing and testing the PR without merging, so the remaining
release obligation stays visible here instead of disappearing.

## Scope

In scope after #v430: remediation of every confirmed release-chain and
privileged-installer finding, independent review, reachable physical hardware
qualification, exact-main CI and crates custody, single-tag release, hosted
installer/signing/notary gates, public-byte audit, homepage deployment, and
final release documentation. The operator explicitly authorized every boundary
on 2026-08-23, including merge, publication, tagging, release, and deployment.

## Plan

Fix the live 4.2.2 release chain first without publishing a new crate, merge
that hardening into the v4.3 candidate, and follow the canonical release
workflow without broad tag pushes or inferred physical acceptance. Merge is
the crates.io publication boundary and cannot occur until the final candidate
is accepted.

## Impact

This task is explicitly authorized to publish immutable external artifacts,
merge code, change repository protections, and deploy the public homepage.
Those actions remain sequenced behind exact-SHA local, hosted, security, and
native-platform gates so authorization never substitutes for acceptance proof.

## Acceptance

**Functional bar:** v4.3.0 is merged and published only from an accepted,
exact-SHA-qualified candidate.

**Evidence bar:** every repository release, native package, signature/notary,
public-byte, and required physical gate is recorded independently.

**Gate ownership:** the operator owns merge/release authorization; repository
policy owns exact-SHA and distribution gates.

## Verification

- [x] Operator explicitly authorizes merge and release execution
- [ ] Every confirmed release/privilege security finding is fixed and rescanned
- [ ] Apple credentials exist only in `apple-signing`; native ARM/Intel preflight passes; repository copies, migration token, and temporary workflow are removed
- [ ] crates.io OIDC and `trustpub_only` pass; the legacy token/secret and bootstrap path are removed; OIDC-only probe passes
- [ ] Final PR head passes independent review, local gates, hosted gates, and reachable hardware qualification
- [ ] Exact-main CI and crates publication resolve for the accepted SHA
- [ ] The single v4.3.0 tag and every release/distribution gate pass
- [ ] Public artifacts and final documentation are audited against immutable bytes

## Status

Active. #v430 is complete and the operator has authorized autonomous security
hardening, qualification, merge, publication, release, and deployment. The
hardening implementation and local exact-commit gate pass on PR #15, while
hosted validation, documentation/state reconciliation, both credential
cutovers, native Apple proof, merge, publication, and public acceptance remain
open and independently blocking.

## Activity

- 2026-08-23 — split from #v430 when the operator authorized PR repair/testing
  but explicitly excluded merge, publication, tagging, and deployment (agent: codex)
- 2026-08-23 — #v430 completed with local/hosted candidate gates and review
  threads green; PR #14 remains open and no merge/release authority is inferred
  (agent: codex)
- 2026-08-23 — operator granted full autonomous authority to remediate the
  release chain, qualify all reachable platforms, merge PR #14, publish crates
  and release assets, and deploy the post-release candidate; moved #r43 to
  Active and began a separate 4.2.2 hardening branch so v4.3 is not published
  before the security boundary is repaired (agent: codex)
- 2026-08-23 — independent red-team review confirmed two additional release
  credential-boundary blockers: Apple signing followed dependency/build
  execution on the same runner, and the long-lived crates.io token followed
  repository execution on the same runner. Expanded the repair to fresh,
  checkout-free credential jobs that accept only fixed prevalidated artifacts;
  no hardening commit, push, merge, or release is allowed before those repairs
  pass adversarial fixtures and another independent review (agent: codex)
- 2026-08-23 — classified the prior scan's remaining low findings. Inherited
  `SHELL`/`PATH` selection while an operator directly runs the report as root is
  a real accepted residual for this release; ordinary report collection does
  not create that privilege boundary. The legacy pathname-based Mac cleanup is
  also retained only for manual/immutable compatibility, while the current PKG
  mutation now belongs to the descriptor-bound native helper. Local task-board
  CSRF/XSS findings remain tracked development-tooling risk and are excluded by
  Cargo's explicit package allowlist (agent: codex)
- 2026-08-23 — completed the profile-metadata repair on both OS families:
  Unix replacements retain an existing private mode before profile bytes are
  written, while Windows existing profiles use fail-closed `ReplaceFileW` so
  DACLs and identity metadata survive the atomic replacement. The native
  Windows regression preserves the original creation metadata and new content
  on the same operation (agent: codex)
- 2026-08-23 — adversarial release review rejected the first workflow rewrite
  before commit. Real v4.2.2 Actions artifacts exposed cargo-dist absolute
  manifest paths and signed/unsigned artifact-set composition assumptions;
  crates.io probes exposed a 403 download response for absent versions; and
  the downstream Mac package builder still ran repository scripts with Apple
  credentials. The revised boundary must prove those real shapes, isolate both
  Mac signing phases, and give a short-lived crates.io OIDC token only to one
  clean Cargo 1.95 publish invocation (agent: codex)
- 2026-08-24 — committed the release-chain hardening as four focused commits,
  opened PR #15, passed the complete locked local gate, and sealed an exact-range
  Codex Security diff scan with no reportable vulnerabilities; hosted and native
  execution remained explicitly open (agent: codex)
- 2026-08-24 — the first hosted PR #15 validation exposed default actionlint
  warnings and credential persistence on nine CI checkouts. Commit `0caa19d`
  repaired both; every one of the 17 workflow checkouts now uses the reviewed
  immutable checkout action with `persist-credentials: false` (agent: codex)
- 2026-08-24 — independent documentation/state review caught premature
  environment-only Apple wording, imprecise “tokenless” language for jobs that
  still use ephemeral read-only GitHub authorization, and the missing active
  release-security task ledger. These are being corrected before the hardening
  PR is pushed again (agent: codex)
