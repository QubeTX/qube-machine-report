TL;DR: Security-harden, independently qualify, merge, publish, and deploy
TR-300 v4.3.0 from one accepted exact candidate.

## Why

The corrected v4.3.0 product candidate passed its original local and hosted PR
gates, but release review identified privileged-installer, credential-custody,
and publication-provenance controls that must be repaired before the candidate
can become a full release. The operator explicitly authorized the complete
repair, merge, publication, release, and deployment campaign.

## Scope

In scope: release-chain and privileged-installer hardening; independent review
and security scanning; Apple and crates.io credential cutovers; reconciliation
of hardened `main` into PR #14; exact-main CI and crates publication; one stable
tag; private draft assembly; native Windows and macOS acceptance; public-byte
audit; post-public updater/Linux/macOS smokes; homepage deployment; and release
ledger closure.

AMD64 Linux laptop, Raspberry Pi, and other physical gates remain independent.
Authorization never substitutes for evidence, and unavailable physical proof
must remain open rather than inferred.

## Plan

1. Land the release-chain hardening through protected PR #15.
2. Complete Apple environment-only and crates.io OIDC-only cutovers, including
   legacy credential and temporary bootstrap removal.
3. Reconcile hardened `main` into PR #14 and repeat exact-head local, hosted,
   security, performance, and reachable-hardware qualification.
4. Merge the accepted exact SHA, prove exact-main CI, and publish the crate by
   explicit OIDC dispatch.
5. Create only `v4.3.0`, keep the GitHub Release private through the exact
   24-to-30-to-validated-to-34 asset chain, and publish only from the macOS
   finalizer after Windows and native Apple acceptance.
6. Audit immutable public bytes and complete post-public smokes before deploying
   the homepage and closing the release ledger.

## Impact

This task is authorized to merge code, change protected hosted credentials,
publish immutable crates/release assets, and deploy the public homepage. Each
irreversible boundary remains sequenced behind its own exact-source evidence.

## Acceptance

**Functional bar:** v4.3.0 is merged and published only from one accepted,
exact-SHA-qualified candidate.

**Evidence bar:** every release, native package, credential, signature/notary,
public-byte, updater, and required physical gate is recorded independently.

**Gate ownership:** the operator owns authority; repository policy owns exact
SHA, credential-custody, native, and distribution acceptance.

## Verification

- [x] Operator explicitly authorizes merge and release execution
- [ ] Every confirmed release/privilege security finding is fixed and rescanned
- [ ] Direct Mac PKG contains only preinstall policy, rejects standard-path
  managed evidence (`~/.cargo/bin/tr300` and
  `~/.config/tr300/tr300-receipt.json`) in every `/Users` home, including
  dot-prefixed unregistered residue, plus all eligible local Directory Service
  homes; fails closed on abnormal or unlistable fixed-path parents; rejects
  non-root target volumes before payload independent of launch environment; and
  passes Intel/ARM rejection plus
  checksum-bound public-v4.2.2-managed refresh and clean/native transition gates
- [ ] Receipt-aware Unix Complete uninstall safely removes an exact managed binary/receipt pair and fails closed on ambiguous ownership
- [ ] PR #15 passes local, hosted, security, and native hardening gates and merges
- [ ] Apple credentials exist only in `apple-signing`; native ARM/Intel proof passes; repository copies, migration token, and temporary workflow are removed
- [ ] crates.io OIDC and `trustpub_only` pass; legacy token/secret and bootstrap code are removed; OIDC-only probe passes
- [ ] PR #14 passes final exact-head local/hosted/security/performance qualification
- [ ] Exact-main CI and explicit crates publication succeed for the accepted SHA
- [ ] The single v4.3.0 tag and private 24-to-30-to-validated-to-34 release chain pass
- [ ] Public artifacts, post-public smokes, homepage deployment, and final ledger are audited

## Status

Active. PR #15 is committed and pushed through `4b33b73`. Its complete local
Rust/package/provenance gate, three exact-range security scans, release-plan
workflow, and 17 of 19 hosted CI checks pass. Both native macOS architectures
now reach the real component-PKG rejection fixture and fail at the same opaque
post-`pkgbuild` assertion. The current focused correction adds PackageKit's
documented `-dumplog`, exact first-case causal proof, phase-specific bounded
failure evidence, and a provenance guard without weakening payload, receipt,
or ownership assertions. Local actionlint, ShellCheck, provenance simulation,
and independent PackageKit/diagnostic reviews pass. A new exact-SHA native
Intel/ARM rerun remains blocking before PR #15 can merge. Credential cutovers,
PR #14 reconciliation, release publication, deployment, and public acceptance
remain independently open.

## Activity

- 2026-08-23 — operator granted full autonomous authority for hardening, merge,
  publication, release, and deployment; began release-chain repair before any
  v4.3 publication (agent: codex)
- 2026-08-24 — committed four focused hardening changes, opened PR #15, passed
  the complete locked local gate, and sealed the first exact-range security
  scan with no reportable vulnerability (agent: codex)
- 2026-08-24 — hosted validation exposed default actionlint warnings and nine
  CI checkouts with persisted credentials. Commit `0caa19d` repaired both; all
  17 workflow checkouts now use the reviewed immutable action with credential
  persistence disabled (agent: codex)
- 2026-08-24 — documentation/state review caught premature Apple environment-
  only wording, imprecise “tokenless” wording for read-only GitHub authorization,
  and the absent active release task. All three are release-blocking until the
  tracked fixes and exact-head revalidation pass (agent: codex)
- 2026-08-24 — exact hosted logs exposed a stale release-path guard, a macOS
  `/bin/true` portability defect, a Linux commit-signal fixture edge, and a
  reproducible concurrent `ReplaceFileW` rollback. Commit `c3338cc` repairs all
  four without weakening custody or metadata guarantees; the unpatched Windows
  race failed within 6–20 iterations, while the corrected transaction passed
  500 consecutive stress iterations plus all 171 unit and 19 integration tests
  (agent: codex)
- 2026-08-24 — the Windows installer gate established that the authenticated
  Inno Setup 6.7.3 `ISCC.exe` intentionally exposes `0.0.0.0` through both
  Windows version-resource fields. Commit `2fba9ba` retains the exact installer
  byte count, SHA-256, GitHub release attestation, and Pyrsys Authenticode
  checks, then uses the authenticated compiler's preprocessor to require exact
  `Ver == EncodeVer(6, 7, 3)` before it can compile repository input. The signed
  6.7.3 compiler passed the positive probe, a 6.7.2 expectation failed closed,
  and the PowerShell parser plus release-provenance fixture passed (agent: codex)
- 2026-08-24 — exact-head hosted run `32708557320` passed the complete Windows
  installer gate (including the authenticated 6.7.3 compiler proof), both Linux
  rollback fixtures, all release builds, Rust tests, audit, cargo-dist, and
  speed gates. It exposed two remaining blockers: four intentionally literal
  release-custody strings lacked scoped ShellCheck `SC2016` annotations, and
  the real-PKG rollback fixture failed after Installer's expected postinstall
  rejection on both native Mac architectures without identifying the failed
  postcondition. Added exact actionlint/ShellCheck coverage plus bounded native
  Installer and assertion diagnostics before the next hosted rerun (agent: codex)
- 2026-08-24 — exact-head hosted run `32710504501` passed workflow validation,
  formatting, clippy, all Linux/Windows tests and release builds, audit,
  cargo-dist, Windows installer custody, and every speed gate at `dae0857`.
  Native Apple Silicon and Intel both returned Installer Code 112 for the
  deliberate postinstall failure, restored the managed binary/receipt exactly,
  and then proved the native payload remained. Because Apple Installer does not
  make that cross-phase operation atomic, automatic managed-to-PKG takeover is
  withdrawn for v4.3. The selected repair is a scoped-home, non-mutating
  preinstall refusal with no postinstall/helper, plus a supported managed-
  installer refresh -> receipt-aware Complete uninstall -> PKG sequence. Native
  upgrade/repair and PKG-to-managed takeover remain required (agent: codex)
- 2026-08-24 — replaced the abandoned Mac postinstall transaction with a
  non-mutating preinstall that scans standard managed binary/receipt paths in
  every `/Users` home, including dot-prefixed unregistered residue, plus all
  eligible local Directory Service homes; enumerates the fixed standard-path
  parents non-recursively and fails closed when they are abnormal or unlistable;
  rejects non-root target volumes; and leaves offline-home recovery fail-closed. Added
  receipt-aware Unix Complete uninstall with confirmation-bound binary/profile
  identity plans, no-clobber staging and rollback, race-safe profile cleanup,
  exact managed receipt removal, and independent adversarial regression review.
  The implementation is committed as `4be2f78` and `015ea73`; the frozen
  pre-commit tree passed the complete Windows-local Rust gate, executable
  provenance fixtures, workflow and shell static checks, managed-installer
  transaction fixtures, and independent final review. Exact-head hosted/native
  qualification remains required before any merge or release boundary
  (agent: codex)
- 2026-08-24 — committed project-state reconciliation as `7000146` and bound
  both prepared Release Apple archives to the exact canonical signed artifacts
  in source custody as `d5c0ce7`. A valid-but-byte-divergent executable archive
  fixture proves the comparison fails closed before publication (agent: codex)
- 2026-08-24 — commit `5d23296` replaces the stale publisher mock with the full
  extracted 30-to-34 asset finalizer. The fixture exposed and repaired three
  Bash `BASH_REMATCH` lifetime defects in macOS and Windows byte verification,
  then added exact jq-filter matching, complete read/mutation classification,
  one exact four-file upload plus visibility-only PATCH, stale-attempt,
  manifest/draft-divergence, artifact-replacement, destination-collision,
  rejected-path deletion, and quoted-string-fidelity regressions. The complete
  provenance suite and two independent reviews passed the exact frozen diff;
  exact-commit local and hosted/native gates remain open (agent: codex)
- 2026-08-24 — commits `76ff364`, `7ede36d`, and `4b33b73` sealed final
  publisher evidence, repaired the hosted ShellCheck trap false positive, and
  corrected exact non-recursive cleanup of root-owned `/Users` fixture roots.
  The new exact-head run passed every non-native gate but both macOS jobs then
  exposed a later silent component-PKG assertion. Independent timing,
  PackageKit-semantics, and observability reviews retained the preinstall
  payload/receipt contract and selected one instrumented rerun: rejecting
  installer calls now request detailed logs, the first case must prove its
  exact managed-owner cause, and any remaining failure reports its phase,
  command, and bounded allowlisted log before cleanup (agent: codex)
