TT;DR: Carry the reviewed v4.4.0 implementation through protected merge, exact-main CI, trusted-OIDC crates publication, immutable tag, and the complete private-to-public release chain. This task waits on `#v440`.

## Why
Repository policy separates implementation readiness from publication. A green local build or PR cannot establish crates custody, immutable tag identity, signed native packages, or the public 34-asset release.

## Scope
In scope: reviewed protected PR merge, exact-main CI, automatic OIDC crates publication, explicit single tag push, private Release/Windows assembly/validation/macOS finalization, and post-public workflow completion. Never move/delete/reuse a failed immutable tag or manually publish a partial draft.

## Plan
Resume after `#v440`: push branch and open PR; resolve review/checks; merge exact head; bind main CI and crates run to merge SHA; tag only after both pass; monitor the complete release chain and fix forward by patch on any non-transient post-tag defect.

## Impact
Publishes a new crate version and signed cross-platform distribution. A post-tag defect consumes the version and requires an immutable patch release.

## Acceptance
**Functional bar:** exact v4.4.0 source is published to crates.io and as a complete stable GitHub Release only after every protected chain stage passes.

**Evidence bar:** recorded PR/head/merge SHA and all exact run IDs, immutable tag identity, native Apple Accepted results, Windows private acceptance, and 34 public nonempty assets.

**Gate ownership:** repository release policy.

## Verification
- [x] Reviewed exact-head PR merges with every required check and thread resolved
- [x] Exact-main CI and automatic trusted-OIDC crates publication pass before tag
- [x] Explicit `v4.4.0` tag triggers successful 24-to-30-to-validated-to-34 publication
- [x] Post-public Windows updater and published Linux/macOS smokes pass

## Status
Done (2026-09-24): v4.4.0 is published at immutable source/tag
`f49d50a256842ac6c677d1c743c553b3dd92717f`, release `396235243`, 34 assets.
Post-public Windows updater run `36087344916` passed all nine jobs; Linux/macOS
published smokes also passed. Protected documentation closure remains in `#q44`.

## Activity
- 2026-08-25 23:01 — created as the immutable publication gate downstream of implementation (agent: codex)

- 2026-09-24 — PR #32/#33 merged; exact-main CI `36084969867` attempt 2 and trusted-OIDC `36084969841` attempt 2 passed. Untagged publisher-policy rejection was fixed before publication.
- 2026-09-24 — Apple preflight `36082271439`, Release `36085847270` (13 jobs), Windows assembly `36086249950`, private validation `36086484931` attempt 2 (16 jobs), and Mac finalizer `36086249996` (12 jobs) passed. Public release created at 2026-09-25T02:41:06Z.
- 2026-09-24 — Public 34-asset audit and Linux/macOS smokes passed; post-public Windows `36087344916` remains in progress. Retry causes and exact checksums are in TESTING.md.

- 2026-09-24 — Post-public Windows `36087344916` passed all nine jobs. Publication task complete; only protected documentation follow-up checks remain.
