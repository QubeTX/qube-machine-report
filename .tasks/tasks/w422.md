# Alienware Global MSI v4.1.3 to v4.2.2 acceptance

TT;DR: Complete one user-approved same-channel UAC update on the Alienware,
then rerun the exact ownership, functionality, and hardware matrix before
releasing the Windows installer lane to ND-300.

## Why

Hosted v4.2.2 Windows jobs prove every installer family and transition in
disposable machines. The Alienware supplies separate evidence for its natural,
long-lived Global MSI registration, machine PATH, terminal behavior, and real
hardware. A cancelled UAC prompt is valid safe-failure evidence but does not
prove the installed transition.

## Plan

Start only while the user is present to approve UAC and after proving no
`msiexec`, Setup/Inno, consent, or updater process exists. Run the installed
`C:\Program Files\tr300\bin\tr300.exe update --json`, accept exactly one UAC
prompt, and require the updater to preserve `msi-global` and Program Files.
From a fresh shell verify v4.2.2, one HKLM Global MSI product, scoped plus legacy
markers, one machine PATH entry, no Corporate/Cargo/portable shadow, no private
live-image backup, and no staging leak. Require current-version update stdout
to be exactly one JSON object. Then rerun table, ASCII, full/fast JSON,
ordinary-no-save, manual-save-and-cleanup, code-page restoration, performance,
and the full Alienware hardware plausibility checks already recorded for
v4.1.3. Do not switch channel, uninstall the active setup, or clean unrelated
files.

## Verification

- [ ] User approves the one UAC ceremony and the update exits successfully
- [ ] Version/path/registry markers/ARP/PATH describe one Global MSI v4.2.2
- [ ] No Corporate/Cargo/portable copy, private backup, staging leak, or duplicate
- [ ] Current update emits exactly one JSON object and reports no update available
- [ ] Table/ASCII/full-fast JSON/save/code-page/performance checks pass
- [ ] Model/BIOS/topology/GPU/disk/memory/battery/boot/network/session remain plausible
- [ ] No MSI/UAC/Setup/updater process remains; hand the idle lane to #nd372

## Status

To-do, blocked only on the user being present for UAC. The release itself is
complete and this is post-release personal-hardware acceptance.

## Activity

- 2026-07-18 — three bounded attempts from the natural v4.1.3 Global MSI
  resolved the public v4.2.2 release and exact tagged Global MSI, then ended as
  UAC cancellation. The final attempt returned exit 2 and exactly one JSON
  object with `install_channel`/`install_origin` `msi-global`, immutable exact
  asset and versionless recovery URLs, and `requires_user_action: true`.
  Installed v4.1.3 bytes/registration remained unchanged and no consent/MSI/
  Setup/TR-300 process remained. Two older, inert TR-300-only temp fixtures were
  inspected and removed by exact path; the final staging scan is empty. No
  attempt crossed channel or created a duplicate. Post-cancellation full/fast
  JSON, Unicode/ASCII, timing, one explicit-save-and-cleanup, and hardware
  plausibility smokes passed on the retained v4.1.3 binary (agent: codex)
