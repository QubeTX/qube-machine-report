TT;DR: After v4.1.3 is public and every hosted gate passes, update the Alienware's natural Global MSI 4.0.1 install and record the final real-Windows evidence without creating another channel.

## Why

Hosted Windows jobs prove disposable installer transitions, while the user's Alienware proves the long-lived real Global MSI registration, machine PATH, hardware collectors, terminal behavior, and absence of duplicates. This must use the public immutable v4.1.3 bytes, not a locally built installer.

## Plan

Confirm the existing Program Files binary is 4.0.1 with `msi-global` origin and one Global MSI registration. Run its updater with JSON capture, accept the single UAC ceremony, and allow the immutable old client to complete or be replaced by its exact same-channel installer. In a fresh shell require 4.1.3 at the same path, one Global registration, scoped plus legacy origin markers, machine PATH, no Corporate/Cargo shadow, no private update backup, and a current-version one-object no-op. Then run table, ASCII, JSON, fast/full, ordinary no-write, manual-save-and-cleanup, console-code-page restoration, failure/recovery, and performance checks. Reconfirm model/BIOS, `6P + 10E`, 16 physical/22 logical cores, both GPUs, disk/memory/battery/boot/network/DNS/locale/session evidence, and record any genuinely unavailable optional field without inventing it.

## Impact

Mutates only the already-authorized TR-300 Global MSI installation in place after the public release is qualified. It must not install Corporate, EXE, Cargo, PowerShell, or portable copies and must not uninstall the active final setup.

## Acceptance

The public update preserves Global MSI/Program Files/machine scope, returns or recovers safely, leaves exactly one current installation, and all functional/hardware checks remain plausible. Exact commands, paths, JSON/exit status, registration, marker, PATH, version, cleanup, performance, and hardware results are recorded in `TESTING.md` and the canonical handoff.

## Verification

- [x] Public v4.1.3 release and hosted updater matrix are green before mutation
- [ ] Natural Global MSI 4.0.1 to public 4.1.3 evidence is complete
- [ ] One-copy/origin/PATH/cleanup and current no-op checks pass
- [x] Public-binary functionality and hardware continuation checks pass

## Status

Active at the explicit user-action boundary. v4.1.3 is immutable and every hosted/public audit gate passed. The natural Alienware baseline remains one Global MSI 4.0.1 in `C:\Program Files\tr300\bin\tr300.exe` with legacy `msi-global`, machine PATH, one ARP product, and no Corporate/Cargo copy. Its updater selected/downloaded/checksum-verified the exact public v4.1.3 Global MSI, then safely returned MSI 1602 failure and retained 4.0.1 when UAC timed out/cancelled. The exact public MSI recovery prompt also timed out without mutation. Resume by launching that same verified Global MSI only when the user can click Yes; never bypass UAC or substitute the portable smoke for installed-origin success.

## Activity

- 2026-07-18 11:15 — task created from the post-v4.1.2 Global live-image finding; preserved the natural baseline for final public-byte validation (agent: codex)
- 2026-07-18 13:30 — public ZIP passed table/ASCII, fast/full JSON, current no-op, no-write/manual-save cleanup, code-page, performance, and full Alienware hardware plausibility. Two Global MSI UAC ceremonies timed out/cancelled safely; installed state remains exactly the preserved 4.0.1 baseline (agent: codex)
