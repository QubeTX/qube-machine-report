Implement and qualify `report` as a full PATH-level alias for `tr300` in v4.4.0.

## Scope and authority
On 2026-09-24 the user explicitly requested shipping the unfinished command,
the MSI EULA repair, and full one-to-one aliases (including install/update/
uninstall) on Windows, macOS, Linux, and every supported install channel.
This supersedes the earlier 2026-08-25 report-only restriction.

Use the clean integration checkout at
C:/Users/hey/.codex/worktrees/tr300-eula-release/qube-machine-report,
implementation branch codex/v4.4.0-eula. Reviewed repairs were integrated
there, then PR #32 and publisher-policy PR #33 merged the published source.

## Acceptance
Every channel carries both commands. `report` forwards all arguments to the
exact adjacent canonical executable without PATH or shell lookup and propagates
exit status. Both names expose identical help/version/options/actions. Native
and managed upgrades from old single-binary installations preserve ownership,
foreign neighboring files, and recoverable rollback state. Windows updates
must handle both loaded images. Both Apple binaries are signed/notarized.
The public release retains 34 stable-name assets and all existing release gates.

## Verification
- [x] Exact-sibling full argument and exit forwarding, missing sibling/PATH isolation
- [x] Cargo/archive/native/managed paired payload and prior-version transitions
- [x] Foreign sibling protection, positive ownership, rollback recovery, loaded-image update
- [x] Version/docs/man pages and full-alias requirements agree
- [x] Full local and hosted release gates plus independent review pass

## Evidence
Original baseline failed compilation with 64 missing identifier errors in
src/migrate.rs. The migration reviewer repaired these and identified rollback
and presence-only ownership bugs. Runtime review identified unsafe verification
that invoked update; packaging review identified old receipt/foreign report
cleanup bugs. All were resolved and verified before production publication.
The EULA RTF and compiled fixture MSI controls passed exact readback checks.

## Status
Done (2026-09-24): implementation and qualification shipped as v4.4.0 at
`f49d50a256842ac6c677d1c743c553b3dd92717f`. Exact-main CI passed all 20 jobs;
paired archives, native installers, Apple signatures/notarization, ownership
and rollback gates passed. Public Windows bytes expose identical full command
help/version and valid reports. Post-public workflow acceptance is complete in `#r44`; documentation closure
remains separately tracked by `#q44`.

## Activity
- 2026-08-25 — initial report-command implementation started and narrowed to report-only.
- 2026-09-24 — resumed after finding uncommitted unfinished work; user authorized full alias behavior on all systems and production minor release.

- 2026-09-24 — PR review findings fixed: strict paired receipt arrays, active-update backup preservation, default Cargo binary, and current alias documentation.

- 2026-09-24 — PR #32 merged `64486fa`; PR #33 repaired the fixed publisher target policy and merged final source `f49d50a`. Exact paired public release published; implementation task completed.
