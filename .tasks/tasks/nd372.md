TT;DR: Record the externally completed ND-300 v3.7.3 Alienware transaction and its decision to keep physical-Mac GUI acceptance optional/deferred behind completed native hosted gates.

## Why

The separate `QubeTX/qube-network-diagnostics` v3.7.3 release is public and its
homepage is deployed. Its previous Alienware native-installer attempt ended at
Windows Installer code 1602 during likely overlapping installer activity. A
physical-Mac batch was initially requested for visible Apple Installer
acceptance. The owning ND task later accepted its completed native Intel/ARM
direct-PKG and legacy-DMG gates as release evidence and classified physical GUI
acceptance as optional/deferred. No ND-300 source file belongs in this TR-300
task.

## Plan

No further action is authorized here. The ND operator-approved transaction
completed before this task's hold reached it, and the owning task reported
there was no live overlap with TR-300. Do not rerun ND Windows Installer or
mutate ND files from this repository.

On the testing Mac, reuse the TR-300 compatibility proof: validate the immutable
legacy DMG SHA sidecar, Gatekeeper result, and stapled ticket; mount read-only;
validate and install its nested PKG; require the expected receipt, payload
paths, versions, and per-file ownership; run the legacy binary's JSON updater;
complete only the standard Apple Installer handoff to the public direct PKG;
then use a fresh shell to recheck the receipt, paired binaries, versions,
ownership, duplicate shadows, and exactly one JSON stdout object. The ND-300
task must supply the exact immutable baseline/tag and public v3.7.3 direct-PKG
URL before this runs; do not guess a superseded asset. Run only `--version`,
`--fast --json`, and `--fast --ascii` non-mutating smoke. Never run `nd300 fix`,
mutate networking, change installer channels, clean unrelated files, or
uninstall the active final setup. Stop safely on any ambiguous prerequisite or
result.

## Impact

This TR-300 task records only the cross-task result and reusable procedure. It
did not change ND files, installers, or networking. Any future optional Mac GUI
batch remains owned by the ND task and requires the user on the testing Mac.

## Acceptance

Tracked evidence records the owning ND task's exact Windows outcome: successful
same-channel update, public-byte hashes, one durable owner, no duplicate or
staging residue, already-latest JSON, migration dry-run, and non-mutating
smokes. It also records that native Intel/ARM direct-PKG and legacy-DMG hosted
gates satisfy release proof while physical GUI acceptance remains optional.

## Verification

- [x] TR-300 v4.2 release work completed and its cancelled UAC lane returned idle
- [x] ND v3.7.2 Global MSI → public v3.7.3 completed exit 0 with no live overlap
- [x] Installed hashes match public bytes; one ARP/marker/machine-PATH owner remains
- [x] No Cargo, retired, staged, or other duplicate remains
- [x] Both already-latest JSON entrypoints, migration dry-run, and read-only smokes pass
- [x] Physical-Mac GUI acceptance is explicitly optional/deferred behind native hosted gates

## Status

Done. The owning ND task completed and verified its Alienware lane before the
hold arrived, reported zero active consent/MSI/updater transactions afterward,
and will not start another Windows installer operation. Physical-Mac GUI
acceptance is optional/deferred; this TR task performed no ND mutation.

## Activity

- 2026-07-18 13:45 — final v3.7.2 physical-Mac acceptance scope received and recorded; no Mac command or ND mutation run (agent: codex)
- 2026-07-18 — v3.7.2 was superseded by public v3.7.3. Added the requested
  post-TR Alienware UAC handoff and codified the reusable legacy-DMG-to-direct-
  PKG acceptance sequence; no ND file, installer, or Mac setup was changed
  (agent: codex)
- 2026-07-18 — TR-300 v4.2.2 release/website closure completed, and a final
  Alienware retry ended in safe UAC cancellation with no remaining installer
  process. Added #w422 as the explicit prerequisite so ND-300 does not overlap
  another TR-300 UAC ceremony; no ND file or installation changed (agent: codex)
- 2026-07-18 — the owning ND task reported its operator-approved v3.7.2 Global
  MSI → public v3.7.3 transaction had already completed exit 0 with no live TR
  overlap. Both installed hashes match public bytes; one ARP/marker/machine-
  PATH owner remains; no Cargo/retired/staged duplicate remains; already-latest
  JSON, migration dry-run, and installed core/technician/ASCII read-only smokes
  pass; zero installer transactions remain. It accepted native Intel/ARM Apple
  gates and deferred optional physical GUI acceptance. No ND action ran here
  (agent: codex)
