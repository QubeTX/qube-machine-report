TT;DR: After TR-300 v4.2 closure and a verified idle installer subsystem, hand off one Alienware UAC lane and one short testing-Mac legacy-DMG-to-direct-PKG acceptance batch for public ND-300 v3.7.3.

## Why

The separate `QubeTX/qube-network-diagnostics` v3.7.3 release is public and its
homepage is deployed. Its previous Alienware native-installer attempt ended at
Windows Installer code 1602 during likely overlapping installer activity. It
also needs final physical-Mac evidence that an immutable legacy DMG updater can
cross safely into the new direct-PKG release while preserving the package
receipt and paired `nd300`/`speedqx` ownership. Hosted matrices are not a
substitute for these bounded user-facing acceptances, but this external-product
task must not interrupt active TR-300 closure or consume borrowed Mac time
early. No ND-300 source file belongs in this TR-300 task.

## Plan

Start only when #v42 and all active TR-300 installer work are complete. On the
Alienware, first prove no `msiexec`, Setup/Inno, UAC ceremony, or other native
installer transaction remains; then return that idle-state evidence to the
ND-300 task for its v3.7.3 Windows Installer/UAC lane. Do not run it from this
TR-300 repository.

On the testing Mac, reuse the TR-300 compatibility proof: validate the immutable
legacy DMG SHA sidecar, Gatekeeper result, and stapled ticket; mount read-only;
validate and install its nested PKG; require the expected receipt, payload
paths, versions, and per-file ownership; run the legacy binary's JSON updater;
complete only the standard Apple Installer handoff to the public direct PKG;
then use a fresh shell to recheck the receipt, paired binaries, versions,
ownership, duplicate shadows, and exactly one JSON stdout object. The ND-300
task must supply the exact immutable baseline/tag and public v3.7.3 direct-PKG
URL before this runs; do not guess a superseded asset. Run only `--version`,
`--fast --json`, and `--fast --ascii` non-mutating smoke. Never run `nd300 fix`,
mutate networking, change installer channels, clean unrelated files, or
uninstall the active final setup. Stop safely on any ambiguous prerequisite or
result.

## Impact

This TR-300 task only records the handoff and idle-installer evidence. The
separate ND task may mutate only its official native installation through the
normal Windows/Apple installer UI. The Mac batch must not begin on the
Alienware, before TR-300 completion, or without the user present for prompts.

## Acceptance

Tracked evidence contains the idle Windows-installer handoff and, from the ND
task, exact commands/results for both v3.7.3 lanes. Mac evidence includes
legacy DMG/direct-PKG Apple trust, receipt/file ownership, updater JSON/exit and
user interaction, fresh-shell resolution, duplicate scan, and non-mutating
smoke without a networking or unrelated-filesystem change.

## Verification

- [ ] TR-300 v4.2 work is complete and no Windows installer/UAC process remains
- [ ] Alienware idle-state evidence and the v3.7.3 Windows lane are handed to the ND task
- [ ] User confirms the separate task is on the testing Mac and supplies its exact immutable legacy baseline
- [ ] Legacy DMG trust, nested receipt, paired paths, versions, and ownership pass
- [ ] Legacy updater opens the public v3.7.3 direct PKG with one JSON stdout object
- [ ] Fresh-shell paths, receipt, duplicate scan, and non-mutating smokes pass

## Status

Queued and deliberately not started. Current host is the Alienware; wait for
#v42 completion, return an idle-installer handoff first, and require explicit
testing-Mac context before the Mac batch.

## Activity

- 2026-07-18 13:45 — final v3.7.2 physical-Mac acceptance scope received and recorded; no Mac command or ND mutation run (agent: codex)
- 2026-07-18 — v3.7.2 was superseded by public v3.7.3. Added the requested
  post-TR Alienware UAC handoff and codified the reusable legacy-DMG-to-direct-
  PKG acceptance sequence; no ND file, installer, or Mac setup was changed
  (agent: codex)
