TT;DR: Reserve one short testing-Mac batch for ND-300 v3.7.1 PKG-in-DMG to public v3.7.2 same-channel update proof after TR-300 is complete.

## Why

The separate `QubeTX/qube-network-diagnostics` v3.7.2 release needs final physical-Mac evidence for the visible Apple Installer handoff and paired `nd300`/`speedqx` ownership. Hosted release/updater matrices are not a substitute for this bounded user-facing acceptance, but this external-product task must not interrupt active TR-300 closure or consume borrowed Mac time early.

## Plan

Start only when #w413 and the active TR-300 work are complete and the user confirms this task is running on the testing Mac. Install the public v3.7.1 universal DMG baseline and require receipt `com.qubetx.nd300.pkg` plus `/usr/local/bin/nd300` and `/usr/local/bin/speedqx`, both reporting 3.7.1 and owned by that package. Run `nd300 update --json`, complete only the standard Apple Installer prompts it opens, then use a fresh shell to require both commands resolve only to `/usr/local/bin`, both report 3.7.2, the receipt is current, no Cargo/archive duplicate shadows either command, and updater stdout is exactly one JSON object. Use the public versionless final URL `https://github.com/QubeTX/qube-network-diagnostics/releases/latest/download/nd300-universal-apple-darwin.dmg`. Run only `--version`, `--fast --json`, and `--fast --ascii` non-mutating smoke. Never run `nd300 fix`, mutate networking, change installer channels, clean unrelated files, or uninstall the active final setup. Stop safely on any ambiguous prerequisite/result.

## Impact

Mutates only the official ND-300 PKG installation through Apple Installer on the testing Mac. It must not begin on Windows, before TR-300 completion, or without the user present for Installer prompts.

## Acceptance

Tracked evidence contains exact commands/results, DMG/PKG Apple trust and receipt/file ownership, v3.7.1 to v3.7.2 updater JSON/exit/user interaction, fresh-shell resolution, duplicate scan, and non-mutating smoke results without a networking or unrelated-filesystem change.

## Verification

- [ ] User confirms the task is on the testing Mac after TR-300 completion
- [ ] Public v3.7.1 DMG baseline, receipt, paired paths, versions, and ownership pass
- [ ] Versionless updater/Apple Installer reaches public v3.7.2 with one JSON stdout object
- [ ] Fresh-shell paths, receipt, duplicate scan, and non-mutating smokes pass

## Status

Queued and deliberately not started. Current host is the Alienware; wait for #w413 completion and explicit testing-Mac context.

## Activity

- 2026-07-18 13:45 — final v3.7.2 physical-Mac acceptance scope received and recorded; no Mac command or ND mutation run (agent: codex)
